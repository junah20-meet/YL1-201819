#1
class Rectangle(object):
    def__init__(self, width, height):
        self.width = width
        self. height = height
    def Area(self):
        return self.width * self.height
    def Perimeter(self):
        return (self.width + self.height) * 2
    def print_properties(self):
        print(self.height)
        print(self.width)

#3
rectangle1= rectangle(15,20)
print (rectangle1.Area())
    print(rectangle1.Primeter())
    class person(object):
        def __init__(self, name, age, city, gemder):
            salf.name= name
            self.age= age
            self.ity = city
            self.gender = gender
        def eat(self, favorite):
            print(self.name, "is eating" + favorite)
        def play(self, fav_sport):
            print(self.name, "is playing" +fav_sport)
person1 = person(juna, 15, nazareth, f)


